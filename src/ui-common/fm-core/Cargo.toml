[package]
name = "fm-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
common = { workspace = true }
async-trait = "0.1"
uuid = { version = "1.7", features = ["v4", "fast-rng"] }
nodeinnet-p2p = { workspace = true, optional = true }
tokio = { workspace = true, optional = true }

[features]
nodeinnet = ["dep:nodeinnet-p2p", "dep:tokio"]
